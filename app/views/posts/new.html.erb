<% title "Create" %>
<blockquote><h2>Create new post</h2></blockquote>

<%= form_for @post, :html => { :class => "form-horizontal" } do |f| %>
	<%= render :partial => "form", :locals => { :f => f } %>
<% end %>

<%= link_to 'back to all post', posts_path, :class => 'btn_back btn btn-inverse btn-large' %> 